## Building and Running

To build and run the JavaFX project using Gradle, follow these commands:

### Build the Project

```bash
./gradlew build
```

### Run the Project

```bash
./gradlew run
```

Make sure you have Gradle installed and set up in your environment. You may also need to ensure that your JavaFX SDK is properly configured in the `build.gradle` file.